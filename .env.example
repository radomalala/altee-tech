# Example environment variables
# Used for Postgres (optional when deploying with a managed DB)
POSTGRES_URL=postgres://USER:PASSWORD@HOST:PORT/DBNAME
POSTGRES_SSL=require

# Email sending (Resend)
# Create an API key at https://resend.com and verify sender
RESEND_API_KEY=your_resend_api_key
RESEND_FROM=onboarding@resend.dev
RESEND_TO=contact@alteetech.com

# Email (Resend)
# Create an API key at https://resend.com
RESEND_API_KEY=your_resend_api_key
# Optional overrides (defaults used if not set)
RESEND_FROM=onboarding@resend.dev
RESEND_TO=contact@alteetech.com